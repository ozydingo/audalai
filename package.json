{
  "name": "audalai",
  "engines": {
    "node": "11.10.0"
  },
  "scripts": {
    "build": "cd client && npm run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run install-client && npm run build && npm run deploy && echo 'Client built!'",
    "install-client": "cd client && npm install && cd .."
  }
}
